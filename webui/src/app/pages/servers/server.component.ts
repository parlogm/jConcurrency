import { Component, OnInit } from '@angular/core';

@Component({
    selector: 's-server-pg',
    templateUrl: './server.component.html',
    styleUrls: [ './server.scss'],
})

export class ServerComponent {
    constructor( ) { }
}
